//! `kestrel-gui` — the native desktop frontend of Kestrel (requirements §6,
//! ADR 0001: Slint shell + sandboxed wry body viewport).
#![cfg_attr(test, allow(clippy::unwrap_used, clippy::expect_used, missing_docs))]
